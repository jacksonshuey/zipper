# Anthropic — used by HaikuRouter and the HTTP API.
ANTHROPIC_API_KEY=

# Optional overrides (defaults shown).
# ZIPPERING_MODEL=claude-haiku-4-5
# ZIPPERING_TIMEOUT_MS=8000
# ZIPPERING_MAX_TOKENS=256
# ZIPPERING_WORKSPACE_KEY=default

# HTTP API: path for the SQLite store (default in-memory).
# ZIPPERING_DB_PATH=./zippering.db
